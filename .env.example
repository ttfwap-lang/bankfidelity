# Bank Statement Fidelity Editor v2.0.0 Configuration
# Copy this file to .env and fill in your API keys.
# .env is gitignored — never commit it.

# =============================================================================
# SECURITY (Required)
# =============================================================================
# Strong passphrase to unlock the pipeline
DUAL_CORE_PASSPHRASE=your_secure_passphrase_here

# =============================================================================
# PDF ENGINE
# =============================================================================
# PyMuPDF Pro License Key (trial keys at https://pymupdf.io/try-pro/)
PYMUPDF_PRO_KEY=your_pymupdf_pro_key_here

# =============================================================================
# LOCAL OCR (Optional — requires `--features ocr`)
# =============================================================================
# To enable Local OCR (ocrs), you need TWO things:
#   1. Build with:  cargo build --features ocr
#   2. Download ONNX model files into the models/ directory:
#      - models/text-detection.rten   (from https://ocrs-models.s3-accelerate.amazonaws.com/text-detection.rten)
#      - models/text-recognition.rten (from https://ocrs-models.s3-accelerate.amazonaws.com/text-recognition.rten)
# Without both files, the UI will show "Local OCR ⛔ Unavailable".

# =============================================================================
# AI PROVIDER — Gemini
# =============================================================================
# Google Gemini API Key (get at https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=your_gemini_api_key

# Auth mode: "api_key" (default) or "vertex" (enterprise, uses SA/ADC)
GEMINI_AUTH_MODE=api_key

# Groq API Key (get at https://console.groq.com/keys)
GROQ_API_KEY=

# OpenRouter API Key (get at https://openrouter.ai/keys)
OPENROUTER_API_KEY=

# Mistral AI API Key (get at https://console.mistral.ai/)
MISTRAL_API_KEY=

# Which AI provider to use for balance analysis, vision, and completeness checks.
# Accepted values (case-insensitive):
#   manual_only   — No AI (default)
#   gemini_api_key / gemini — Google Gemini via API key
#   gemini_vertex  / vertex — Google Gemini via Vertex AI
#   groq_api_key   / groq   — Groq (fast Llama 3)
#   openrouter_api_key / openrouter — OpenRouter (DeepSeek)
#   mistral_api_key / mistral — Mistral AI
# This is also persisted by the GUI's Backend Preferences panel.
AI_PROVIDER=manual_only

# =============================================================================
# DOCUMENT PARSERS
# =============================================================================

# --- Reducto (DEFAULT cloud parser) ---
# Primary extraction backend; also used for layout-agnostic transaction
# transfers. Get at https://platform.reducto.ai/
REDUCTO_API_KEY=

# --- Mindee (optional legacy parser) ---
# V2 API key (get at https://platform.mindee.com/)
MINDEE_API_KEY=
# V2 Extraction Model UUID
MINDEE_MODEL_ID=

# --- LlamaParse (LLM-based parser) ---
# API key (get at https://cloud.llamaindex.ai/)
LLAMAPARSE_API_KEY=

# --- Google Document AI ---
DOCUMENT_AI_PROJECT_ID=your_gcp_project_id
DOCUMENT_AI_LOCATION=us
DOCUMENT_AI_PROCESSOR_ID=your_processor_id

# Auth: Service account JSON path. Not needed if you ran
# `gcloud auth application-default login`.
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Document AI API key (Beta v1beta3 endpoint). ADC/SA act as fallbacks.
# Get at https://console.cloud.google.com/apis/credentials
DOCUMENT_AI_API_KEY=

# --- Document Parser Selection ---
# NOTE: the active parser is chosen in the GUI (Settings -> Backend
# Preferences) and persisted there; this env var is NOT read by the app.
# Code default is Reducto, with fallbacks Document AI -> LlamaParse ->
# offline heuristic.
DOCUMENT_PARSER_MODE=

# =============================================================================
# ENVIRONMENT RUNTIME
# =============================================================================
# Absolute path to a Python interpreter with PyMuPDF installed (Python 3.12-3.14).
# Example: C:\path\to\python.exe
PYTHON_EXECUTABLE=

# =============================================================================
# VERIFICATION
# =============================================================================

# pdfRest — Adobe-tier cloud PDF rendering for visual diff
# Get at https://pdfrest.com/ → Dashboard → API Keys
PDFREST_API_KEY=

# =============================================================================
# TELEMETRY & LOGGING (Optional)
# =============================================================================
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
OTEL_SERVICE_NAME=dual-core-pdf-pipeline
RUST_LOG=info
LOG_DIR=./logs

# ---------------------------------------------------------------------------
# Microsoft UFO UI-agent integration (optional)
# ---------------------------------------------------------------------------
# Install root for Microsoft UFO. Override if UFO lives outside C:\UFO.
BANKFIDELITY_UFO_DIR=C:\UFO
# Optional explicit Python interpreter used to launch UFO. Resolution order:
# PYO3_PYTHON > PYTHON_EXE > BANKFIDELITY_UFO_PYTHON > UFO bundled python_env > PATH python.
# BANKFIDELITY_UFO_PYTHON=

# =============================================================================
# ADDITIONAL OPTIONAL BACKENDS & RUNTIME TOGGLES
# (all optional; safe to leave unset - offline fallbacks always apply)
# =============================================================================

# Local LLM (offline Qwen bridge). Default: http://127.0.0.1:11434/v1
LOCAL_LLM_URL=http://127.0.0.1:11434/v1
# Optional: model tag, bearer token, timeout (defaults keep the Qwen2.5-Coder-7B setup)
# LOCAL_LLM_MODEL=qwen2.5-coder-7b-instruct-q4_k_m
# LOCAL_LLM_API_KEY=
# LOCAL_LLM_TIMEOUT_SECS=90

# Local vision-language model (OpenAI-compatible, e.g. vLLM on a GX10).
# Additive evidence only; both URL and MODEL must be set to enable it.
# LOCAL_VLM_URL=http://gx10.local:8000/v1
# LOCAL_VLM_MODEL=
# LOCAL_VLM_API_KEY=
# LOCAL_VLM_TIMEOUT_SECS=300


# Vision AI verification backend (greys out of the UI when unset)
VISION_API_KEY=

# LiPI lookup integration
LIPI_API_KEY=

# Model overrides (sane defaults applied when empty)
OPENROUTER_MODEL=mistralai/mistral-nemo:free
MISTRAL_MODEL=mistral-large-latest

# Telemetry / notification hooks (optional)
SENTRY_DSN=
WEBHOOK_URL=

# Runtime behaviour toggles (optional; defaults are sensible)
# auto = PyMuPDF Pro Primary (the default fidelity editor rank).
PDF_ENGINE_MODE=auto
PDFIUM_AUTO_DOWNLOAD=
AUTO_MATCH_DPI=
FORCE_HEADLESS_FALLBACK=
INTERACTIVE_FALLBACKS=
TRANSFER_CONSENSUS_MODE=
