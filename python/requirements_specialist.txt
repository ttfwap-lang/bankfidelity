# BankFidelity specialist environment (OPTIONAL; separate from the hash-pinned
# worker runtime in runtime-manifest.json, so its Python version is independent).
# Tested target: Python 3.11-3.12. On the GX10 (aarch64 + CUDA) install torch from
# NVIDIA's aarch64 CUDA wheels/container instead of the generic PyPI build, and
# serve large models with vLLM rather than loading them in-process here.

# Core Deep Learning & Tensor Operations
torch>=2.2.0
torchvision>=0.17.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.3
einops>=0.8.0
timm>=1.0.8

# Layout analysis (opt-in via BF_SPECIALIST_SURYA=1; bridge targets the legacy
# surya.model.ordering API and reports "unavailable" on newer releases)
surya-ocr>=0.5.0
opencv-python-headless>=4.10.0
numpy>=1.26.0
pillow>=10.4.0

# Typography & vector geometry
fonttools>=4.53.0
pymupdf>=1.24.0
pdfplumber>=0.11.0

# Differential verification & math
scipy>=1.13.0
scikit-image>=0.23.0

# IPC & async bridge
pydantic>=2.7.0
httpx>=0.27.0
